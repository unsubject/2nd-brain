# Handoff — 2026-05-18 pm

Session started from a confused state: "I tried to work on goals yesterday but the MCP shows nothing." Diagnosed → designed → shipped 3 PRs and seeded the founding constitution.

## What shipped

| PR | Title | Merge SHA |
|---|---|---|
| #53 | `journal_entry.last_error` (mirror of #43) | `baf3847` |
| #54 | Constitution split: 4-layer goal hierarchy | `7ab0ba3` |
| #55 | MCP `resources/*` capability for protocol docs | `e804b7b` |

Migration 016 (journal `last_error`) and 017 (constitution split) both applied to live Railway PG via the 2nd-brain-pg MCP. `_migrations` table reflects both.

5 constitution domains seeded directly via SQL (Mind / Body / Family / Wealth / Social) with paired `constitution_amendments` rows (kind='new', status='committed'). **Founding bypass slot fully used** — counter is at 5/5, all future `kind='new'` constitution amendments face the 14-day cooldown.

## Decisions worth keeping in mind

These are NOT self-evident from the diff:

1. **Founding bypass widened 3 → 5** (constitution layer). PR #54's `commit_constitution_amendment.ts` has `FOUNDING_BYPASS_LIMIT = 5` with the rationale baked in as a comment. Sized for Mind/Body/Family/Wealth/Social. Irreversible counter — retire/merge don't refund.

2. **`undertakings.outcome` column intentionally kept misnamed.** After the split, undertakings produce *output*, not *outcome* (which now lives on `goals`). Rename to `output_target` would cascade through 5 files + docs. Tracked as a Build item; do when the misnomer starts costing more than it saves.

3. **No founding bypass at the goals layer.** Goal proposals can be staged in parallel; total bootstrap latency is ~3 days regardless of count. The 72h cooldown without bypass is the right friction for SMART-goal sanity.

4. **Goals can't re-parent via `amend`.** `constitution_domain_id` is immutable. To move a goal between domains: `abandon` + `new`. Two audit-log entries make the change visible; an `amend` would silently morph meaning.

5. **Resources over tools for protocol docs.** PR #55 wires `resources/list` + `resources/read`. Single source of truth: `docs/goal-amendment-interview.md` is imported directly into the Worker bundle via wrangler's `Text` rule. Tech-lead caught that the deploy workflow's `paths:` filter didn't include `docs/...`, which would have caused silent doc/bundle drift — fixed in same PR with explicit per-file allowlist.

## Dead ends / things tried and abandoned

- **PR #53 first review claimed an `updated_at = now()` drift vs PR #43.** I verified empirically (read `markArtifactError` from main): no drift, both bump `updated_at` on error. Did NOT push the proposed fix. If a future reviewer raises the same point, the answer is the same. Treat as a worked example: passing self-tests aren't evidence the advice is wrong, but primary-source counter-evidence is.

- **Considered inline TypeScript string for the protocol doc in PR #55.** Rejected because of backtick-escape pain on a 7KB markdown doc + duplication cost. Cross-directory `import` from `docs/` is the right pattern.

## Open follow-ups (now on the Build list)

- `verify journal_entry.last_error in prod after PR #53` — pending real error to fire
- `fix worker outer-catch clobbering processed rows on downstream failure` — pre-existing, made more visible by PR #53
- `rename undertakings.outcome → output_target` — constitution-split carry-over
- `update docs/mcp-behavior-and-dev-norms.md for 4-layer model` — was out-of-scope for PR #54
- `dispatcher unit tests for resources/list + resources/read` — PR #55 tech-lead nit
- `narrow wrangler.jsonc Text glob to explicit allowlist` — PR #55 tech-lead long-term flag; trigger is "when 2nd resource added"

## State for the next session

- DB: migrations 016 + 017 applied; 5 active `constitution_domains`; 0 rows in `goals`/`goal_amendments`/`undertakings`/`undertaking_cycles`.
- Code: main is at `e804b7b` (post-PR #55). All three PRs deployed via Railway (monolith) + Cloudflare Worker (mcp-worker).
- User's next move: write the first SMART goals via claude.ai. claude.ai's MCP needs a disconnect/reconnect to see the new tool surface (`list_constitution_domains`, `propose_goal_amendment`, etc.) and the new `resources/list` capability.

## How to start the next session

Most likely opener: "where were we with goals?" — Resume mode. Build has 2+ open items tagged `#2nd-brain`. Read this handoff for context, then either continue follow-ups or pivot.
