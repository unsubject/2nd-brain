# 2026-05-11 — 2nd-brain session

## What shipped

- **PR #37** (merged, main at `614fd7c`): removed orphan `POST /capture` route + `randomUUID` import in `src/bot.ts`. Renamed `CAPTURE_API_KEY` → `ARCHIVE_API_KEY` in `bot.ts` and `.env.example`. Railway env var swapped in lockstep with deploy (add new → merge → delete old).

## Correction to the 2026-04-24 plan

The previous handoff suggested removing `CAPTURE_API_KEY` along with the `/capture` route. That was wrong — `startWebhook()` in `bot.ts` also reads it as the Bearer-auth check for `/archive/*` (admin/import HTTP routes used by external callers, not by the bots). Renamed instead. Lesson: when a handoff lists an env var as orphan, grep the full file before deleting — the same name can guard more than one surface.

Also: the PR description initially framed `/archive/*` as "family-bot RAG endpoints," which is wrong. Family bot's `/ask` calls `askFamilyArchive()` in-process. `/archive/*` HTTP routes are admin/import (Notion sync, search, diagnostics, stats, retry-errors).

## What surfaced (unrelated to PR #37)

- **OpenAI account ran out of quota** mid-session. Caught when verifying family-bot `/ask` post-deploy: `RateLimitError: 429 insufficient_quota` in `[familyAsk] Error`, failing at `generateEmbedding`. Initial diagnosis went wrong because the symptom appeared right after my deploy — timing was coincidence, not causation. Topped up OpenAI billing → resolved.
- Blast radius reminder: `insufficient_quota` silently breaks every OpenAI-dependent path — captures (`processor.ts` `gpt-5.4-nano`), archive analyze/entities, embeddings, both bots' `/ask`. Only morning review (Anthropic Sonnet 4.6) is unaffected.
- Post-outage check: `SELECT COUNT(*) FROM captures WHERE state = 'processing_error' AND received_at > '2026-05-10'` returned 0. No stuck captures to retry this time.

## Still open from 2026-04-24

Deferred again this session because the quota incident ate the time. Both are observational, not coding:

1. **Verify post-#34 captures**: send a test journal capture and confirm `[archive] analyze tokens in=… out=…` log lines in Railway show real numbers, not `undefined`. The 2026-04-24 handoff specifically flagged this risk after the OpenAI usage-field rename (`input_tokens/output_tokens` → `prompt_tokens/completion_tokens`).
2. **Observe Sonnet 4.6 morning review**: next 06:00 local. Watch for quality + verbosity. Knob if too long: `max_tokens` cap in `src/review.ts`.

## Notes for next session

- Railway env: `CAPTURE_API_KEY` was deleted after the green deploy. Only `ARCHIVE_API_KEY` is live.
- The add-new → merge → delete-old env-var cutover worked clean. Worth reusing for any future env-var rename in this app.
- When a user reports a failure right after deploy, sanity-check the failing code path against the diff before assuming causation. The `/ask` symptom here was unrelated to a `bot.ts` rename.
